---

# project info
key: A_PROJ
name: Android_Projects

# repositories
repos:
## CWD PIQ
  - name: cwd_piq
    label: android
    build_command: bash build.sh
    environment:
      SRVM_CUSTOMER_IDS: 504
      SRVM_RELEASE_FOR: PM
      SRVM_RELEASE_BY: howard_chu
      SRVM_PRODUCT_CATALOG: kalay cam
      BUILD_PLATFORM: android
      BUILD_OUTPUT_PATH: output
      REPORT_PATH: reports
    email_list: 
      - howard_chu@tutk.com
## KalayVSaaS
  - name: tutk_kalay_vsaas
    label: android
    build_command: bash build.sh
    environment:
      SRVM_CUSTOMER_IDS: 504
      SRVM_RELEASE_FOR: PM
      SRVM_RELEASE_BY: howard_chu
      SRVM_PRODUCT_CATALOG: vsaas
      BUILD_PLATFORM: android
      BUILD_OUTPUT_PATH: output
      REPORT_PATH: reports
    email_list: 
      - howard_chu@tutk.com
## WaraHome
  - name: tutk_warahome
    label: android
    build_command: bash build.sh
    environment:
      SRVM_CUSTOMER_IDS: 504
      SRVM_RELEASE_FOR: PM
      SRVM_RELEASE_BY: howard_chu
      SRVM_PRODUCT_CATALOG: kalay cam
      BUILD_PLATFORM: android
      BUILD_OUTPUT_PATH: output
      REPORT_PATH: reports
    email_list: 
      - howard_chu@tutk.com
## KalayDoorPhone
  - name: tutk_kalay_doorphone_2.0
    label: android
    build_command: bash build.sh
    environment:
      SRVM_CUSTOMER_IDS: 504
      SRVM_RELEASE_FOR: PM
      SRVM_RELEASE_BY: eddie_chen
      SRVM_PRODUCT_CATALOG: DoorPhone
      BUILD_PLATFORM: android
      BUILD_OUTPUT_PATH: output
      REPORT_PATH: reports
    email_list: 
      - howard_chu@tutk.com
      - eddie_chen@tutk.com