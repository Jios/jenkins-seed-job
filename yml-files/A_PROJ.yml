---

# project info
key: A_PROJ
name: Android_Projects

# repositories
repos:
  - name: cwd_piq
    label: android
    build_command: fastlane build
    environment:
      SRVM_CUSTOMER_IDS: 504
      SRVM_RELEASE_FOR: PM
      SRVM_RELEASE_BY: howard_chu
      SRVM_PRODUCT_CATALOG: kalay cam
      BUILD_PLATFORM: android
      BUILD_OUTPUT_PATH: output
      REPORT_PATH: reports
    email_list: 
      - howard_chu@tutk.com
  - name: tutk_kalay_vsaas
    label: android
    build_command: fastlane build
    environment:
      SRVM_CUSTOMER_IDS: 504
      SRVM_RELEASE_FOR: PM
      SRVM_RELEASE_BY: howard_chu
      SRVM_PRODUCT_CATALOG: vsaas
      BUILD_PLATFORM: android
      BUILD_OUTPUT_PATH: output
      REPORT_PATH: reports
    email_list: 
      - howard_chu@tutk.com